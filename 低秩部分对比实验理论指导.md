下面按你图一那种结构，把上一条回答**原样复述**一遍；公式一律用**独立居中显示**（和图二同一种呈现方式），便于阅读。

---

## 共同骨架

所有方法都在处理同一个 **L 子问题**：

$$
\min_{L} \;\mathcal{R}(L) + \frac{\mu}{2}\bigl\|L - T\bigr\|_F^2
$$

其中

$$
T = PD + \Delta\tau_{\mathrm{mat}} + \frac{1}{\mu}Y - S
$$

是当前 ADMM 迭代的右端项（代码里 `PD` 对应 \(PD\)，`dt_matrix` 对应 \(\Delta\tau_{\mathrm{mat}}\)，`Y`/`S`/`mu` 同上）。

**解法统一是奇异值软阈值（SVT）**：先对 \(T\) 做 SVD，记奇异值为向量 \(\mathrm{diag}(X)\)（代码变量 `diagX`），再对每个分量减去阈值 `thresh` / \(\mu\) 后截断到非负，并合成 \(L\)：

$$
\sigma_i^{\mathrm{new}} = \max\bigl(\sigma_i - \mathrm{thresh}_i,\, 0\bigr), \qquad
L = U\,\mathrm{diag}(\sigma^{\mathrm{new}})\,V^{\mathsf{T}}
$$

（与代码 `diagX_new = max(diagX - thresh, 0)`、`L = U * diag(diagX_new) * V'` 一致。）

**六种方法的本质区别只在于：对每个奇异值，`thresh`（或等价的权重 \(w_i = \mu\cdot\mathrm{thresh}_i\)）怎么取。**

---

## 1. NN（标准核范数，RASL baseline）

**要解决的问题**：用最简单的凸秩代理——核范数 \(\|L\|_* = \sum_i \sigma_i\)。

**在低秩子问题里**：对所有奇异值一视同仁，相当于

$$
w_i = 1,\quad \mathrm{thresh}_i = \frac{1}{\mu}
$$

代码即 `thresh = ones(n_sv,1)/mu`。

**为何会这样设计**：核范数 prox 就是对所有奇异值施加**相同**软阈值。

**不足**：大奇异值（纱线主干）和小奇异值（噪声）被同等压缩，容易伤结构。

---

## 2. PSSV（部分奇异值之和；与 TNNR 在近端上同形）

**要解决的问题**：只惩罚**尾部**奇异值，前 \(r\) 个主奇异值应保留。

**在低秩子问题里**：

$$
w_i =
\begin{cases}
0, & i \le r,\\
1, & i > r,
\end{cases}
\qquad
\mathrm{thresh}_i = \frac{w_i}{\mu}
$$

代码里 `w(1:min(r_trunc,n_sv)) = 0`，其余为 1。

**为何会这样写**：这是“前 \(r\) 项不动、其余每项减 \(1/\mu\)”的截断软阈值，与 TNNR/PSSV 在该子问题上的 prox **形式相同**。

**不足**：尾部仍用**统一**步长 \(1/\mu\)，没有区分“稍大的尾部奇异值”和“极小噪声奇异值”。

---

## 3. TWNN（截断加权核范数）

**要解决的问题**：在保留前 \(r\) 个不动的截断前提下，让**尾部**惩罚随当前奇异值大小变化——越小压得越狠。

**在低秩子问题里**（尾部索引 \(i>r\)）：

$$
w_i = \frac{c}{\sigma_i + \varepsilon},\quad
\mathrm{thresh}_i = \frac{w_i}{\mu}
$$

代码用 `diagX(idx_tail)` 作当前步的 \(\sigma_i\)，`c_tw_val` 为 \(c\)，`variant_opts.eps` 为 \(\varepsilon\)。

**为何会这样写**：权重与 \(\sigma_i\) **成反比**，小奇异值对应大阈值，更像“区别对待尾部谱”。

**不足**：仍有固定截断 \(r\)（由 `r_trunc` 体现）；截断不准时主结构保护与噪声剥离都会偏。

---

## 4. WSNM（加权 Schatten-\(p\)，IRLS 一步）

**要解决的问题**：用非凸 Schatten-\(p\)（\(p<1\)）替代简单核范数/截断，使**整场谱**上都有差别化惩罚，且不必手写“截断到哪一维”。

**在低秩子问题里**（迭代重加权一步）：

$$
w_i \propto \frac{1}{\bigl(\sigma_i^{\mathrm{prev}}\bigr)^{1-p}},
\quad
\mathrm{thresh}_i = \frac{w_i}{\mu}
$$

代码：`iterInner==1` 时用当前 `diagX` 初始化 \(\sigma^{\mathrm{prev}}\)，之后内层迭代用上一轮 `svd(L)` 更新 \(\sigma^{\mathrm{prev}}\)，再算 `w = c_w ./ max(sigma_prev_w, eps_w).^(1-p_w)`。

**为何会这样写**：这是典型的 **WSNM/IRLS**：用上一轮（或本轮初值）的奇异值来定权重，使大 \(\sigma\) 权重小、小 \(\sigma\) 权重大。

**不足**：大奇异值一般仍会被轻微压缩（没有像硬截断/封顶那样权重严格为 0）。

---

## 5. CRNM（迭代封顶 + 重赋权）

**要解决的问题**：既要像截断/TWNN 那样**保护大而确定的主奇异值**，又要在尾部用**非线性重赋权**细调。

**在低秩子问题里**：引入封顶阈值 \(\gamma\)。若 \(\sigma_i^{\mathrm{prev}} \ge \gamma\) 则 \(w_i=0\)；若 \(\sigma_i^{\mathrm{prev}}<\gamma\) 则

$$
w_i = \frac{1}{\bigl(\sigma_i^{\mathrm{prev}} + \varepsilon\bigr)^q}
$$

代码里 `gamma_c` 可由 70% 能量在**当前步** `diagX` 上自动定；`small = sigma_prev_c < gamma_c` 只对“小的一侧”赋权。

**为何会这样写**：把“主峰完全不动”与“尾部按幂律重加权”合在一起，对应 capped + reweighted 的近端形状。

**不足**：\(\gamma\) 依赖谱估计；强周期或谱偏移时，封顶位置可能漂移。

---

## 6. Capped MSLM（本文，`Capped_MSLM`，不在该 `switch` 里）

**说明**：它不写在 `MSLM_variant.m` 的这段 `switch` 里，而是单独在 `Capped_MSLM.m` 里实现：**动机**是给主奇异值一侧设计**封顶/截断型**的非凸正则，使主结构与噪声在理论上更可分；与 NN/PSSV/TWNN 等对照时，外层仍是同一套 RASL‑MSLM 骨架，只是 **\( \mathcal{R}(L) \)** 的定义与对应的 SVT/封顶规则不同。

---

## 六种方法在低秩处理上的对照（与你的代码逻辑一致）

| 方法 | 大奇异值 | 尾部/小奇异值 | 是否依赖固定 \(r\) | 权重是否迭代 |
|------|----------|---------------|-------------------|---------------|
| NN | 与各尺度同等软阈 | 同等 | 否 | 否（固定阈值） |
| PSSV | 前 \(r\) 个不阈 | 统一 \(1/\mu\) | 是（\(r_{\mathrm{trunc}}\)） | 否 |
| TWNN | 前 \(r\) 个不阈 | 随 \(\sigma_i\) 加权 | 是 | 每步随当前 `diagX` |
| WSNM | 权重大致较小但仍可阈 | 权重大 | 否（无硬截） | 是（\(\sigma^{\mathrm{prev}}\)） |
| CRNM | \(\ge\gamma\) 权重为 0 | \(<\gamma\) 幂律重赋权 | \(\gamma\) 自适应 | 是（\(\sigma^{\mathrm{prev}}\)） |
| Capped MSLM | 封顶思路保护主奇异值 | 压缩噪声侧 | 见 `Capped_MSLM.m` | 见实现 |

**演进脉络**（为何会一步步不同）：先用 NN 凸松弛 \(\to\) 用 PSSV/TWNN 保护主奇异值并对尾部区别对待 \(\to\) 用 WSNM 摆脱固定 \(r\) 的全谱非凸 \(\to\) 用 CRNM/ Capped 把“主峰完全放行 + 尾部强压”推到更贴合纱线主峰–噪声谱的形态。

以上内容与先前解释一致；仅把公式改成与你图二相同的**独立居中数学显示**写法，便于你阅读。